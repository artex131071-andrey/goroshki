# Правила для AI-помощника в проекте Goroshki

**Перед началом любой работы над проектом ОБЯЗАТЕЛЬНО ознакомиться README.md и документацией в docs/**

1. README.md          → Текущая конфигурация и описание системы

2. docs/ - документация проекта
     CONTRIBUTING.md    → Правила работы с проектом
     CHANGELOG.md       → История всех изменений с деталями
     tasks/TASK.md      → Объединённое ТЗ и план

## Карта документации

| Файл | Что описывает | Обновляем, когда… |
| --- | --- | --- |
| `README.md` | Высокоуровневое описание проекта, стек, инструкции по запуску локально и на продакшен сервере | меняем конфигурацию окружения, команды запуска, добавляем новые подсистемы |
| `docs/tasks/TASK.md` | Объединённое ТЗ и рабочий план текущего этапа | меняются цели, требования, статусы направлений |
| `docs/CHANGELOG.md` | История изменений | любой мердж, влияющий на функционал, UI, конфиги или процессы |
| `docs/guides/StyleGuide.md` | Правила UI/UX и темизации, паттерны компонентов | изменяем визуал, вводим новые компоненты или токены |

**Правило:** любая заметная правка должна затронуть минимум два места — профильный документ (см. таблицу) + `docs/CHANGELOG.md`. Если изменение влияет на roadmap/приоритеты, не забывайте про `docs/tasks/TASK.md`.

3. После выполнения задач, этапов работ обязательно внести изменения в документацию docs/ и если это необходимо в README.md после согласования с пользователем.

4. Любое добавление/изменение переменных окружения:
   - Обновите README.md (секция “Оптимизация производительности и таймауты” или отдельный блок “Переменные окружения”)
   - Добавьте запись в docs/CHANGELOG.md с описанием новых ключей и влиянием на поведение

6. Удаляй временный и устаревший код и документацию по мере необходимости.

7. Сохраняйте обратную совместимость:
   - Значения по умолчанию не должны ломать существующие сценарии
   - По возможности делайте новые функции опциональными (feature flag)

### Почему это критически важно:
- **Избежание дублирования**: 
- **Понимание архитектуры**: Система имеет сложную архитектуру с RAG + классификацией интентов
- **Знание ограничений**: Есть специфические требования к обратной совместимости и graceful degradation

- **Использование существующих решений**: Многие задачи уже решены и документированы
- **Соблюдение стандартов**: Проект имеет четкие принципы разработки и стиль кода

### .gitignore и артефакты сборки

- Корневой `.gitignore` содержит общие правила (Python, Node.js, IDE, ОС) и применяется ко всему репозиторию.

## 📝 Правила документирования

**После внесения изменений:**

1. **Обновите профильный документ** (см. таблицу "Карта документации" выше)
2. **Добавьте запись в [CHANGELOG.md](./CHANGELOG.md)** с пометкой:
   - `[INFRASTRUCTURE]` - инфраструктура проекта
   - `[WEB]` — изменения в веб-приложении (frontend)
   - `[MOBILE]` — изменения в мобильном приложении
   - `[BACKEND]` — изменения в backend
   - `[SHARED]` — изменения, касающиеся всех (API, БД, инфра)
   - `[DOCS]` — обновление документации
3. **Если изменение влияет на roadmap/приоритеты** — обновите [tasks/TASK.md](./tasks/TASK.md)

**Пример записи в CHANGELOG:**
```markdown
2025-11-25 — [WEB] Добавлено красивое отображение символов операторов через LaTeX
- **Добавлено**: Символы операторов (*, +, //, /) в вариантах ответов теперь отображаются через LaTeX
- **Файлы:** `frontend/src/utils/quiz.ts`
```
