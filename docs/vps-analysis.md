# VPS Анализ и План подготовки (2026-01-20)

## 📊 Текущее состояние сервера

### Системные характеристики
| Параметр | Значение |
| --- | --- |
| **ОС** | Ubuntu 24.04.3 LTS |
| **Kernel** | 6.8.0-90-generic |
| **Архитектура** | x86_64 (AMD EPYC 9654) |
| **Оперативная память** | 1.9 Gi (720Mi используется, 1.2Gi доступно) |
| **Диск** | 29G (2.7G используется, 26G свободно) |
| **Swap** | 0B (не настроен) |

### Установленное ПО

| ПО | Версия | Статус | Примечание |
| --- | --- | --- | --- |
| **Nginx** | 1.24.0 (Ubuntu) | ✅ Активен | Работает, настроен как основной веб-сервер |
| **PHP** | 8.3.6 (CLI) | ⚠️ Только CLI | PHP-FPM НЕ установлен - требуется для WordPress |
| **MySQL** | 8.0.44 | ✅ Активен | Готов, оптимально для WooCommerce |
| **Git** | 2.43.0 | ✅ Установлен | Репозиторий уже клонирован в `/var/www/goroshki/` |
| **curl** | 8.5.0 | ✅ Установлен | - |
| **wget** | 1.21.4 | ✅ Установлен | - |

### Firewall (UFW)
- **Статус**: ✅ Активен
- **Открытые порты**:
  - 22 (SSH)
  - 80 (HTTP)
  - 443 (HTTPS)

### Структура проекта на сервере
```
/var/www/
├── goroshki/           ← Наш проект (репозиторий уже клонирован)
│   ├── .git/
│   ├── .gitignore
│   ├── README.md
│   └── vps-status.txt
└── html/               ← Папка по умолчанию для статических сайтов
```

### Конфигурация Nginx
- Default конфиг существует в `/etc/nginx/sites-available/default`
- Sites-enabled пуст (только default)
- Требуется создать отдельные конфигурации для двух доменов

---

## 🚨 Выявленные проблемы и требуемые действия

### КРИТИЧЕСКИЕ (обязательно решить):

1. **PHP-FPM не установлен** ❌
   - PHP CLI установлен, но PHP-FPM (модуль для обработки PHP через Nginx) отсутствует
   - **Решение**: Установить `php-fpm` версии 8.3
   - **Влияние**: WordPress не будет работать без этого

2. **SSL сертификаты не настроены** ❌
   - HTTPS требуется для производства (443 порт открыт, но сертификаты не готовы)
   - **Решение**: Настроить Let's Encrypt SSL сертификаты (Certbot)
   - **Рекомендация**: Установить Certbot для автоматического обновления

3. **Swap память не настроена** ⚠️
   - При нехватке оперативной памяти сервер может зависнуть
   - **Решение**: Добавить swap файл (рекомендуется 2Gi)

### ВАЖНЫЕ (желательно решить):

4. **Nginx конфиги не настроены под два домена** ❌
   - Требуется создать отдельные конфиги:
     - `/etc/nginx/sites-available/goroshki.ru` (статический лендинг)
     - `/etc/nginx/sites-available/shop.goroshki.ru` (WordPress + WooCommerce)
   - Требуется настроить reverse proxy между ними

5. **Документация WordPress не установлена** ❌
   - WordPress, WooCommerce и плагины нужно установить
   - Требуется подготовка БД для WordPress

6. **Оптимизация MySQL для WooCommerce** ⚠️
   - Текущая конфигурация базовая
   - Рекомендуется настроить параметры для оптимальной работы с WooCommerce

---

## 📋 План подготовки сервера

### Фаза 1: Основная подготовка (КРИТИЧЕСКИ ВАЖНО)

- [ ] 1.1 Обновить список пакетов: `apt update && apt upgrade -y`
- [ ] 1.2 Установить PHP-FPM 8.3: `apt install -y php8.3-fpm php8.3-mysql php8.3-gd php8.3-curl php8.3-json php8.3-zip`
- [ ] 1.3 Установить Certbot для Let's Encrypt: `apt install -y certbot python3-certbot-nginx`
- [ ] 1.4 Добавить swap файл: 2Gi рекомендуется для стабильности
- [ ] 1.5 Перезагрузить сервер для применения всех изменений

### Фаза 2: Конфигурация Nginx

- [ ] 2.1 Создать конфиг для `goroshki.ru` (статический лендинг):
  ```
  /etc/nginx/sites-available/goroshki.ru
  - Root: /var/www/goroshki/landing/
  - Тип: статический (HTML/CSS/JS)
  - SSL: Let's Encrypt
  ```

- [ ] 2.2 Создать конфиг для `shop.goroshki.ru` (WordPress):
  ```
  /etc/nginx/sites-available/shop.goroshki.ru
  - Root: /var/www/goroshki/shop/
  - Backend: PHP-FPM socket
  - SSL: Let's Encrypt
  - WooCommerce конфиг
  ```

- [ ] 2.3 Включить конфиги в sites-enabled
- [ ] 2.4 Тестировать и перезагрузить Nginx

### Фаза 3: Подготовка к WordPress

- [ ] 3.1 Создать БД для WordPress: `goroshki_shop`
- [ ] 3.2 Создать БД пользователя с правами
- [ ] 3.3 Скачать и распаковать WordPress
- [ ] 3.4 Установить WooCommerce и необходимые плагины

### Фаза 4: SSL и финализация

- [ ] 4.1 Выпустить SSL сертификаты через Certbot
- [ ] 4.2 Настроить автоматическое обновление сертификатов
- [ ] 4.3 Проверить редирект с HTTP → HTTPS

---

## 📝 Рекомендации

### Безопасность:
- Регулярно обновлять систему: `apt upgrade`
- Монитор логов: `/var/log/nginx/` и `/var/log/mysql/`
- Создать регулярные резервные копии БД

### Производительность:
- Настроить кэширование Nginx для статических файлов
- Включить OPcache в PHP (уже включен в 8.3)
- Оптимизировать MySQL параметры для WooCommerce

### Мониторинг:
- Настроить логирование ошибок Nginx и PHP-FPM
- Мониторить использование памяти и диска
- Тестировать время отклика сервера

---

## ❓ Вопросы для уточнения:

1. Где хранится лендинг (`goroshki.ru`)? В репозитории или нужно его загрузить?
2. Какие плагины требуются для WooCommerce кроме базовых?
3. Есть ли backup старой версии данных?
4. Требуется ли резервное копирование по расписанию?
5. Нужна ли CDN для статических файлов?
